<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="public/styles.css">
  </head>
  <body>
    <section class="robots-section">
      {{#users}}
        <section class="robots">
          <img src="{{avatar}}">
          <a href="http://localhost:3000/{{username}}"><h2>{{name}}</h2></a>
          {{#job}}
            <p>{{job}}, {{company}}</p>
          {{/job}}
          {{^job}}
            <p class="not-working">Available for hire</p>
          {{/job}}
        </section>
      {{/users}}
    </section>

  </body>
</html>
